apiVersion: v1
kind: Secret
metadata:
  name: medifollow-secrets
type: Opaque
data:
  # Base64 encoded environment variables for Mongo Atlas and Cloudinary
  # (Example values. We will securely inject real ones via Jenkins later)
  # FRONTEND_URL: staging UI origin (e.g. http://NODE_IP:30001) — inject via Jenkins secret k8s-staging-frontend-url; use one URL for email links (comma-list only if you accept broken magic links)
  MONGODB_URI: bW9uZ29kYjovL21vY2stY2x1c3Rlci5uZXQvbWVkaWZvbGxvdz8=
  CLOUDINARY_URL: Y2xvdWRpbmFyeTovL21vY2stY2xvdWRpbmFyeQ==
